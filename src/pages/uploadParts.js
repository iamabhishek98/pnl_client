import React from "react";
import UploadPartsContainer from "../containers/uploadPartsContainer";

const uploadParts = (props) => {
  return <UploadPartsContainer {...props} />;
};

export default uploadParts;
