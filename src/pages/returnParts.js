import React from "react";
import ReturnPartsContainer from "../containers/returnPartsContainer";

// to add home page
const returnParts = (props) => {
  return <ReturnPartsContainer {...props} />;
};

export default returnParts;
